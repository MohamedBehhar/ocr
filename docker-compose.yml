services:
  ocr-engine:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 120s
